# ──────────────────────────────────────────────────────────────
# Chuka University Timetabling System — Python dependencies
# Install: pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────

# Core framework
Django>=4.2,<5.0

# Static file serving in production (correct MIME types, compression, caching)
whitenoise>=6.6.0

# Database driver — MySQL (production default)
mysqlclient>=2.2.0

# REST API
djangorestframework>=3.14.0

# API throttling / abuse protection (login, search, feedback, notifications)
django-ratelimit>=4.1.0

# Import / Export (django-admin bulk data tools)
django-import-export>=3.3.0

# PDF generation
reportlab>=4.1.0
weasyprint>=60.0

# Image handling
Pillow>=10.0.0

# Data analysis / export helpers
pandas>=2.0.0
openpyxl>=3.1.0

# Password-protected Excel/CSV export & import (Programs page)
msoffcrypto-tool>=5.0.0
pyzipper>=0.3.6

# HTML parsing (used in feedback chat view)
beautifulsoup4>=4.12.0

# HTTP requests (notification webhooks, external APIs)
requests>=2.31.0
urllib3>=2.0.0

# AI Smart Importer — document parsing (CSV/Excel work without these;
# PDF/Word extraction needs them only when an AI provider is configured)
pdfplumber>=0.10.0
python-docx>=1.1.0

# Encryption for stored AI provider API keys (core.ai_registry)
cryptography>=42.0.0

# AWS S3 media storage (optional — set USE_S3=true in .env)
boto3>=1.34.0

# WSGI server (production)
gunicorn>=21.0.0

# Static file serving in production
whitenoise>=6.6.0

# CORS support (required by corsheaders app/middleware in settings.py,
# used for the Expo mobile/web app talking to this API)
django-cors-headers>=4.3,<5.0
# ── Backup, Disaster Recovery & Audit System ──────────────────────────
# Celery: optional but recommended for production background processing.
# On PythonAnywhere without Redis, tasks auto-fall back to daemon threads.
celery>=5.3,<6.0
redis>=5.0,<6.0
django-celery-beat>=2.5,<3.0

# Optional backup destinations — install only what you use:
# boto3>=1.34                 # AWS S3 (already present above)
# azure-storage-blob>=12.19   # Azure Blob Storage
# google-cloud-storage>=2.14  # Google Cloud Storage
# paramiko>=3.4               # SFTP destinations
