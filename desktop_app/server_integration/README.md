The Django side of the desktop sync now lives in the project root as `desktop_sync/` (already installed in
`INSTALLED_APPS` and `urls.py`). See `desktop_sync/README.md` for the API, permissions and tests.
